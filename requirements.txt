# ============================================================
# YOLOv1 From Scratch — 项目依赖
# ============================================================
# 安装方式：pip install -r requirements.txt
# 建议使用 conda 或 venv 创建独立环境后安装
# ============================================================

# ============================================================
# 核心框架
# ============================================================
torch>=2.0.0
torchvision>=0.15.0

# ============================================================
# 数据处理 & 计算
# ============================================================
numpy>=1.21.0
Pillow>=9.0.0        # PIL — 图像加载 (dataset/voc_dataset.py)
opencv-python>=4.7.0 # cv2  — 推理可视化、视频帧处理 (Phase 6/7)
scipy>=1.9.0         # IoU 计算、距离矩阵、SORT 追踪 (Phase 6/7)
lxml>=4.9.0          # 高性能 XML 解析，替代内置 xml.etree (可选)
pandas>=1.5.0        # 训练日志、评估指标 CSV 导出与分析

# ============================================================
# 可视化
# ============================================================
matplotlib>=3.5.0    # Loss/mAP 曲线、检测结果绘图

# ============================================================
# 训练 & 日志
# ============================================================
tqdm>=4.64.0         # 训练进度条
tensorboard>=2.12.0  # TensorBoard 训练日志可视化
pyyaml>=6.0          # 配置文件解析 (config.yaml)

# ============================================================
# 推理 & 追踪 (Phase 6/7)
# ============================================================
filterpy>=1.4.5      # SORT 追踪所需的卡尔曼滤波器
scikit-image>=0.19.0 # 图像预处理、形态学操作

# ============================================================
# 开发 & 测试
# ============================================================
pytest>=7.0.0        # 单元测试
